<%= form_for [@post, @comment] do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Comment me..." %>
  </div>
  <%= f.submit "comment", class: "btn btn-danger" %>
<% end %>
