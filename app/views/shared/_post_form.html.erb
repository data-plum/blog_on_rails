<%= form_for(@post) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Post me..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-danger" %>
<% end %>
